import 'package:flutter/material.dart';

class MedialysPage extends StatelessWidget {
  const MedialysPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Medialys'),
      ),
    );
  }
}
