import 'package:flutter/material.dart';

class SejahteraMedikaPage extends StatelessWidget {
  const SejahteraMedikaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sejahtera Medika'),
      ),
    );
  }
}
