import 'package:flutter/material.dart';

class ModernCarePage extends StatelessWidget {
  const ModernCarePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Modern Care'),
      ),
    );
  }
}
