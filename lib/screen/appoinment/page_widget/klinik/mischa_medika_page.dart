import 'package:flutter/material.dart';

class MischaMedikaPage extends StatelessWidget {
  const MischaMedikaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mischa Medika'),
      ),
    );
  }
}
